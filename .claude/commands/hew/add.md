---
description: Add a new task to the existing graph.
---

Parse the task description from the user. Use bd create with the right type + priority + parent. Wire dependencies if obvious. Print bd dep tree of the affected subtree before confirming.
