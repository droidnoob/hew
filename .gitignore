.planning/
.beads/
.hew/loop/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# vhs tapes — local only, regenerate GIFs with `vhs assets/<name>.tape`
assets/*.tape
